(ns plastic.boot
  (:require [plastic.node]))

; -------------------------------------------------------------------------------------------------------------------
; intentionally empty, see boot.coffee